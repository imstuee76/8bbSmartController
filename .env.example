# Git push / updater
GITHUB_TOKEN=replace_me
GITHUB_REPO=owner/repo
GITHUB_REPO_NAME=8bbSmartController
GIT_BRANCH=main

# Controller defaults (optional)
# Set this to your Windows flasher host when controller runs on Linux (example below).
CONTROLLER_BACKEND_URL=http://192.168.1.50:8088
CONTROLLER_AUTH_TOKEN=
