This repo is local-first and persistence-sensitive.

Rules:
- Never auto-delete files under `/data`.
- Prefer additive migrations.
- Keep credentials encrypted/hashed where possible.
- Maintain landscape touch usability (large targets and spacing).
- Do not break offline LAN usage.
